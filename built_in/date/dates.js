#!/usr/bin/node
// Writing dates in javascript
const now = new Date(); // Assign the date to variable now
console.log(now); // prints the exact date and time

console.log(now.valueOf()); // Prints the number of miliseconds