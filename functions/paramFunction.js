#!/usr/bin/node
// declare function and add parameters
function myFunction(param1, param2) {
	consloe.log(param1);
	console.log(param2);
}
// add some arguments into the function
myFunction(20, 'some string');

// call the function, will print 20 for param1 and some string for param2
myFunction();
