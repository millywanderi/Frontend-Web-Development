#!/usr/bin/node
// Writing a ananymous function
const anotherFunction = function () {
	console.log('another function');
}
// check it's type. It returns function
typeof anotherFunction

// call the function it prints another function
anotherFunction();
