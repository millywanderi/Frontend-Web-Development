#!/usr/bin/node

/*
  const does not allow redeclaration of a variable.
  It gona print an error message if you redeclare const
*/

const newVariable = 29;

// print the variable
console.log(newVariable);
