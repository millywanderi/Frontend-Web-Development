#!/usr/bin/node

/*
  let is used whenever you expect to reassign a variable in a code.
  One can reassign the variables multiple times.
*/

let counter = 0;

// reassign the counter variable
counter = 1;

// reassign again
counter++;

// print the results
console.log(counter);
