#!/usr/bin/node

// addition
const x = 20;
const y = 30;

const result = x + y;

console.log('The sum is:', result);

// substruction
const a = 50;
const b = 30;

const subs = a - b;

console.log('The substruction is:', subs);

// division
const m = 90;
const n = 30;

const divide = m / n;

console.log('The division is:', divide);

// multiplication
const i = 40;
const j = 40;

const multiply = i * j;

console.log('The multiplication is:', multiply);

// exponent
const k = 8;
const l = 8;

const exp = k ** l;

console.log('The exponent is:', exp);

// modulus
const c = 9;
const d = 2;

const mod = c % d;

console.log('The modulus is:', mod);

// increment
let startingNumber = 1;

startingNumber++;

startingNumber++;

console.log('The increment is:', startingNumber);

// decrement
let endingNumber = 9;

endingNumber--;

endingNumber--;

console.log('The decrement is:', endingNumber);
