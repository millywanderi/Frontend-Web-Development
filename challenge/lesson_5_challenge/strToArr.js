#!/usr/bin/node
// Write a function to split a string and convert it into an array of words.

function stringToArray(string){
    const arr = string.split(' '); // converts the string to an array and separates with spaces
    return arr;
}